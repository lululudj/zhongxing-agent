# zhongxing 🧠

**人语→机语：让小模型拥有长文本问答能力的语义压缩管道**

在 i5-12600K + GTX1060 6GB + 16GB RAM 的老设备上，通过轻量级 NLP 预压缩和结构化聚合，让 14B 模型获得长文本深度问答能力。

## 核心理念

**压缩不是删减，是编码。**

人类语言充满冗余——修饰词、程度副词、因果连接词、等级模式……这些对人类理解有帮助，但对模型推理是噪声。zhongxing 把"人语"转译成"机语"——零歧义的结构化表示，让小模型用最少的 token 完成推理。

核心洞察：**不是让小模型够用，而是通过压缩管道技术，让 256B 这种超大模型也能在老设备上快速稳定运行。** 输入极小，KV Cache 极轻，瓶颈从显存转移到语义保真度。

## 架构

```
原文 → 分块 → [小脑×3 并行提取] → RRF融合+紧凑化 → 补漏+共现扫描 → 机语
                                                                          ↓
问题 → 大脑(14b) ← 机语(紧凑行格式) → 自然语言回答（含反馈环）
```

### 三层智能体

| 层 | 模型 | 职责 | 驻留内存 |
|---|------|------|---------|
| 小脑 ×3 | qwen2.5:1.5b | 并行提取实体/属性/关系/因果 | 0.75GB |
| 中脑 | qwen2.5:3b | 人名识别(NER) + 维度自发现 | ~3GB |
| 大脑 | qwen2.5:14b | 读取机语推理，自然语言回答 | ~9GB |

### 管道流程

1. **维度自发现** — 3b 自动推断文本类型（小说/论文/法律/…）、实体类型、属性维度、关系类型
2. **分块提取** — 长文本自动按段落边界切分为 ~900 字/块，每块 3 个 1.5b 并行提取
3. **RRF 融合** — Reciprocal Rank Fusion 排序 + 模糊名字去重 + 子串去重降噪
4. **确定性紧凑化** — 30+ 条通用规则覆盖 8 大类冗余模式（虚词/副词/修饰词/等级/因果/被动/动词短语/尾缀）
5. **补漏校验** — 3b 列人名 + 模糊匹配补漏 + 共现关系扫描
6. **大脑推理** — 14b 读取紧凑行格式机语，含反馈环（缺失信息从原文补充）

## 紧凑行格式

机语采用紧凑行格式而非 JSON，格式开销从 30% 降到 5%：

```
[韩枫] 叛徒弟子 | 师徒,弟子,师父 | 偷袭→灵魂遁入骨灵冷火
[药老] 师父 | 朋友,徒弟,不肖弟子,师父 | 灵魂体遁入骨灵冷火
[萧炎] 修炼师 | 徒弟,师傅,朋友 | 偷袭→灵魂遁入骨灵冷火
```

vs JSON 等价：
```json
{"E":[{"n":"韩枫","A":["叛徒弟子"],"R":["师徒","弟子","师父"],"C":["偷袭→灵魂遁入骨灵冷火"]},...]}
```

## 测试结果

### 短文本（539字，斗破苍穹节选，3题）

| 版本 | 正确率 | 压缩比 | 耗时 |
|------|--------|--------|------|
| v1.5.9 | 3/3 ✅ | 2.1:1 | 26.1s |
| v1.6.0 | 3/3 ✅ | 2.2:1 | 22.1s |
| **v1.6.1** | **3/3 ✅** | **2.5:1** | **22.0s** |

### 长文本（4441字，斗破苍穹扩展，8题）

| 版本 | 正确率 | 压缩比 | 耗时 | 关键改善 |
|------|--------|--------|------|---------|
| v1.5.9(无分块) | ~3/8 | 6.6:1(虚高) | 201s | 1.5b 提取崩塌 |
| v1.6.0(分块) | ~4/8 | 3.9:1 | 169s | 提取恢复，属性串门 |
| **v1.6.1** | **~5/8** | **3.8:1** | **164s** | 字符拆解/垃圾实体/子串去重 |

### 空context对照实验（Hermes）

- 14B 空 context 直接答：幻觉严重（编造"美杜莎是希腊神话"等完全不存在内容），9s/题但答案不可用
- 管道压缩 context 模式（14s/题）：所有答案都在上下文范围内不编离谱内容
- **结论：准确率提升来自压缩检索，不是模型自带知识**

## 快速开始

### 依赖

- Python 3.8+
- [Ollama](https://ollama.ai/) + 以下模型：
  - `qwen2.5:1.5b` — 提取器（3个并行）
  - `qwen2.5:3b` — NER + 维度自发现
  - `qwen2.5:14b` — 大脑推理

```bash
ollama pull qwen2.5:1.5b qwen2.5:3b qwen2.5:14b
ollama serve
```

### 运行

```bash
# 内置测试（539字短文本）
python zhongxing_agent.py --test

# 长文本测试
python zhongxing_agent.py --file examples/test_long.py

# 交互模式（输入文本后反复提问）
python zhongxing_agent.py --interactive
```

### 自定义测试

创建一个 Python 文件，定义 `LONG_TEXT` 和 `LONG_QUESTIONS`：

```python
LONG_TEXT = """你的长文本内容..."""

LONG_QUESTIONS = [
    "问题1",
    "问题2",
]
```

然后运行：
```bash
python zhongxing_agent.py --file your_test.py
```

## 紧凑化规则（8大类，30+条）

| 类别 | 示例 |
|------|------|
| 虚词/判断词 | 乃是→空, 则是→空, 拥有→空 |
| 程度副词 | 极其→空, 非常→空, 异常→空 |
| 通用修饰 | 强大的→空, 特殊的→空, 过人的→空 |
| 等级模式 | 斗皇级别的强者→斗皇 |
| 因果链 | 导致→→, 使得→→, 造成→→ |
| 被动/迫使 | 被迫→空, 遭到→被 |
| 动词短语 | 提升实力→↑实力, 不修炼也能→免修 |
| 尾缀清理 | 之中→空(句尾), 之内→空(句尾) |

## 已知限制

- **属性串门**：1.5b 在长文本提取时偶尔将属性安错实体（如"厄难毒体"出现在萧炎行而非小医仙行），需要更精确的实体-属性绑定机制
- **压缩比 vs 准确率权衡**：分块后压缩比从 6.6:1 降至 3.8:1，但准确率显著提升
- **14b 推断不可控**：让 14b 做跨实体推断会导致幻觉，当前仅做读取机语 + 直接推理
- **通用性待验证**：当前测试集中在小说文本，论文/法律/财报等场景需要额外测试集

## 版本历史

### v1.6.1 (2026-06-11) — 分块+降噪
- 修复字符拆解 bug（1.5b 输出字符串而非列表时逐字拆开）
- 修复垃圾实体名（"名"等 prompt 模板字面输出）
- 新增子串去重（短属性是长属性子串时删短留长）
- normalize_items 统一归一化 A/R/C 字段

### v1.6.0 (2026-06-11) — 分块提取
- 新增 chunk_text() 自动分块（~900字/块，按段落边界切分）
- 共现扫描改用分句窗口（逗号/分号切分）+ 最近 2 实体
- 去掉"女王/家主"身份词噪声

### v1.5.9 (2026-06-11) — 稳定版
- 去掉身份词噪声，3/3 ✅, 2.1:1, 26.1s
- 新增 --file 模式和 run_test_with() 通用测试函数

### v1.5.7 (2026-06-11) — 共现扫描首次 3/3
- 确定性共现关系扫描，解决"朋友"关系丢失问题

### v1.5 (2026-06-11) — 紧凑序列化
- 紧凑行格式替代 JSON，格式开销从 30% 降到 5%
- 30 条通用 compactify() 规则
- 砍掉 3b 融合模型（删实体/篡改事实/展开属性）

## License

MIT

## 致谢

- [Ollama](https://ollama.ai/) — 本地模型推理
- [Qwen2.5](https://qwen.readthedocs.io/) — 提取/NER/推理模型
